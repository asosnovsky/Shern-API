<!doctype html>
<html>
<head>
	<meta charset="utf-8"/>
	<title>Shern</title>
	{{!-- Public (CSS) --}}
	{{!-- Public (CSS) --}}

	{{!-- BOWER --}}

		{{#setBower 'css'}}
			<link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
		{{/setBower}}

		{{#setBower 'js'}}
		{{/setBower}}
		
	{{!-- BOWER --}}
</head>
<body>
	{{{body}}}
</body>
</html>